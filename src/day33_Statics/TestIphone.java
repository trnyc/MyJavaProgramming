package day33_Statics;

public class TestIphone {
    public static void main(String[] args) {
        /*
    to call and use 'static' variables in another class, just you need to type class name and put a dot (.)
    the list of static variables will show up. BUT when you need to use instance variables, you MUST use
    objects.
     */


        System.out.println(Iphone.brand);




 /* Static method only accepts static members. In static method you can not use any instance.
 (see slyte no:7). Static method can share what the class has. And objects are created from class,
 classes are not created from the objects. ınstances are belong to objects, not class.
  */







    }
}
